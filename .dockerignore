# Dependências e build — reinstalados/regerados dentro da imagem
node_modules
.next
out
build
coverage

# Git e CI
.git
.github
.gitignore

# Env — injetados via runtime (compose/hPanel), nunca na imagem
.env
.env.*
!.env.local.example
!.env.example

# Editor / OS
.DS_Store
.vscode
.idea

# Docker (evita auto-cópia recursiva)
Dockerfile
.dockerignore
docker-compose.yml

# Docs e misc não necessários em runtime
*.md
.claude
.prettierignore
.prettierrc
.editorconfig
